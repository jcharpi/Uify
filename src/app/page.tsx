import { theme } from '../styles';

export default function Home() {
  return (
    <div style={{
      minHeight: '100vh',
      backgroundColor: theme.colors.active.default
    }}>
    </div>
  );
}
